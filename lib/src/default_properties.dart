import 'package:flutter/material.dart';

const double _defaultRadius = 12;
const double _largeBorderRadius = 28;
const double _largeBorderRadiusBusiness = 20;
const double defaultPadding = 16;

const homeScreenSnackBarPadding = EdgeInsets.only(bottom: 70, left: 16, right: 16, top: 16);

const defaultRadius = Radius.circular(_defaultRadius);
const largeRadius = Radius.circular(_largeBorderRadius);
final BorderRadius defaultBorderRadius = BorderRadius.circular(_defaultRadius);
final BorderRadius largeBorderRadius = BorderRadius.circular(_largeBorderRadius);
final BorderRadius largeBorderRadiusBusiness = BorderRadius.circular(_largeBorderRadiusBusiness);

const EdgeInsets defaultPaddingHorizontal = EdgeInsets.symmetric(horizontal: defaultPadding);
const EdgeInsets defaultPaddingAll = EdgeInsets.all(defaultPadding);
